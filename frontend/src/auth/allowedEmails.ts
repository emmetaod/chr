// Add Gmail addresses that are allowed to access the Reports page.
// Use '*' as the only entry to allow any authenticated Google account.
const ALLOWED_EMAILS: string[] = [
  '*',
  // 'example@gmail.com',
]

export default ALLOWED_EMAILS
